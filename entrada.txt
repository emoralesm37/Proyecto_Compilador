program {
    int x;
    int y;
    int w;
    int z;
    int a;
    int b;
    x = 10;
    y = 20;
    z = x + y * 2;          // z = 50
    a = 20 + 40 / 2 * 10 - 100;
    if (z > 30 && x != 0) {
        w = z / 2;
    } else {
        w = z - 5;
    }
    b = a + w;
}